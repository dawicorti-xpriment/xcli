#!/usr/bin/env node

var _ = require('lodash');
var program = require('commander');
var generator = require('../lib/generator');

program
  .version('0.0.1')
  .command('header', 'xcli-add-header')
  .command('lib', 'xcli-add-lib')
  .parse(process.argv);
